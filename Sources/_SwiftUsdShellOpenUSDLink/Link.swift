// Empty by design. This target carries the OpenUSDNative product into the
// SwiftUsdShellOpenUSD product so the prelinked adapter's native references
// resolve without force-loading a second copy of its Swift overlay metadata.
enum SwiftUsdShellOpenUSDNativeLink {}
